# 🚀 Быстрый старт

## ✅ Что уже готово:
- ✅ Все миграции созданы
- ✅ Все модели созданы  
- ✅ Все контроллеры созданы
- ✅ Все API роуты настроены
- ✅ База данных настроена на `ecommerce_shop_db`

## 📋 Что нужно сделать ВАМ:

### 1️⃣ Создайте базу данных MySQL

Откройте MySQL и выполните:
```sql
CREATE DATABASE ecommerce_shop_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2️⃣ Настройте файл .env

Если файла `.env` нет, скопируйте `.env.example` в `.env`:
```bash
copy .env.example .env
```

Затем откройте `.env` и настройте подключение к БД:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ecommerce_shop_db
DB_USERNAME=root
DB_PASSWORD=ваш_пароль_mysql
```

### 3️⃣ Установите зависимости (если еще не установлены)
```bash
composer install
```

### 4️⃣ Сгенерируйте ключ приложения
```bash
php artisan key:generate
```

### 5️⃣ Запустите миграции
```bash
php artisan migrate
```

### 6️⃣ Запустите сервер
```bash
php artisan serve
```

## 🎉 Готово!

API будет доступно по адресу: **http://localhost:8000/api**

### Первый тест:

**Регистрация пользователя:**
```
POST http://localhost:8000/api/register
Content-Type: application/json

{
  "name": "Тест",
  "email": "test@test.com",
  "password": "password123",
  "password_confirmation": "password123"
}
```

## 📚 Документация:
- `README_API.md` - Полная документация всех API endpoints
- `SETUP.md` - Подробная инструкция по установке

---

**Если что-то не работает - проверьте:**
1. MySQL запущен
2. База данных создана
3. Данные в .env правильные
4. Миграции выполнены успешно

